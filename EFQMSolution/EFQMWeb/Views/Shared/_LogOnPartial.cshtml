@if(Request.IsAuthenticated) {
    <text>Dobrodošli <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Odjava", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Prijava", "LogOn", "Account") ]
}
